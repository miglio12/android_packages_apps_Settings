<?xml version="1.0" encoding="UTF-8"?>

<PreferenceScreen
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:settings="http://schemas.android.com/apk/res/com.android.settings"
    android:title="@string/general_settings_title">

    <PreferenceCategory
        android:title="@string/general_settings_style_title">

	<!-- Recents RAM bar -->
  	<PreferenceScreen
            android:key="recents_ram_bar"
            android:fragment="com.android.settings.temasek.RamBar"
            android:title="@string/ram_bar" /> 

        <!-- Alternate App picker -->
        <CheckBoxPreference
            android:key="use_alt_resolver"
            android:title="@string/alt_resolver"
            android:summary="@string/alt_resolver_summary"
            android:defaultValue="false" />

	<!-- CPU info overlay -->
	<CheckBoxPreference
            android:key="show_cpu_info"
            android:title="@string/show_cpu_info"
            android:summary="@string/show_cpu_info_summary"/>

        <!-- Restart SystemUI -->
        <Preference android:key="restart_systemui"
            android:title="@string/restart_systemui" />

        <!-- MediaScanner behavior on boot -->
        <ListPreference
            android:key="media_scanner_on_boot"
            android:title="@string/media_scanner_on_boot_title"
            android:persistent="false"
            android:entries="@array/media_scanner_on_boot_entries"
            android:entryValues="@array/media_scanner_on_boot_values" />

        <!-- Screen state service -->
        <PreferenceScreen
            android:id="@+id/screen_state_service"
            android:fragment="com.android.settings.temasek.fragments.ScreenStateToggles"
            android:icon="@drawable/ic_screen_state"
            android:title="@string/screen_state_toggles_title"
            android:summary="@string/screen_state_toggles_summary" />

        <!-- Wakelock blocker -->
        <PreferenceScreen
            android:id="@+id/wakelock_blocker"
            android:fragment="com.android.settings.temasek.fragments.WakeLockBlocker"
            android:icon="@drawable/ic_wakelock_blocker"
            android:title="@string/wakelock_blocker"
            android:summary="@string/wakelock_blocker_header_summary" />

    </PreferenceCategory>

</PreferenceScreen>
